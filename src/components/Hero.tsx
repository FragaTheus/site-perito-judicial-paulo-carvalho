import { Stack } from "@mui/joy";
import { Section } from "./Section";

export const Hero = () => {
  <Section id="hero" bgcolor="transparent">
    <Stack flex={1} bgcolor={"primary.100"}>
      teste
    </Stack>
  </Section>;
};
